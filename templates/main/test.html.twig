<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>test</title>
</head>
<body>
    {#
        Ceci est un commentaire twig qui
        ne s'affiche pas dans le navigateur
    #}

    <h1>Page de test</h1>

    {{"Petit texte pour tester"}}

    {% set city = "Rennes" %}
    <p>{{ city }}</p>

    {# Checker la valeur de la variable #}
    {% if city == "Rennes" %}
        <p>Ville de Rennes </p>
    {% elseif city == "Nantes" %}
        <p>Ville de Nantes</p>
    {% else %}
        <p>Pas une ville répertoriée</p>
    {% endif %}

    {% set vegetables = ["Carotte", "Tomate", "Patate"] %}

    <h1>Légumes :</h1>
    {% for vegetable in vegetables %}
        <p>{{ vegetable }}</p>
    {% else %}
        <p>Pas de légumes</p>
    {% endfor %}

    <p>Ingrédient à l'honneur : {{ vegetables[2] }}</p>

    {%
        set user = {
            'name' : 'jojo',
            'age' : 10,
            'isactive': true
        }
     %}

    <h1>{{ user.name }}</h1>
    <dl>
        {% for key, val in user %}
            <dt>{{ key }}</dt>
            <dd>{{ val }}</dd>
        {% endfor %}
    </dl>

</body>
</html>